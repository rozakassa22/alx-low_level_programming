#include "main.h"
/**
 * mul - a function that multiply two int
 * @a: an int to be multiplied
 * @b: an int to be multiplied
 * Return: the product of two int
 */
int mul(int a, int b)
{
	return (a * b);
}
